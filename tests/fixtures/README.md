# Fixtures

This directory is reserved for future fixture repositories, sample diffs, and prompt rendering tests.

The plugin is intentionally generic, so fixtures should avoid depending on any single language or framework.
